<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Shizuku permission for real flavor -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!-- Override Shizuku's minSdk requirement -->
    <!-- Shizuku requires API 23+, but we handle this at runtime -->
    <!-- We check Build.VERSION.SDK_INT >= 23 before using Shizuku -->
    <uses-sdk tools:overrideLibrary="rikka.shizuku.provider,rikka.shizuku.api,rikka.shizuku.shared,rikka.shizuku.aidl,rikka.sui" />

    <application>
        <!-- Shizuku Provider for real Shizuku API support -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>

